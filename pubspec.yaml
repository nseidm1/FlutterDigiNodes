name: diginodes
description: diginodes
version: 1.0.1+4

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  socket_io: ^0.9.0+2
  crypto: any
  cryptoutils: any
  hex: ^0.1.2
  esys_flutter_share: ^1.0.1
  archive: ^2.0.8
  flutter_launcher_icons: ^0.7.0
  bitcoin:
    git: https://github.com/nseidm1/dart-bitcoin.git
  flutter:
    sdk: flutter

flutter_icons:
  android: "logo"
  ios: true
  image_path: "assets/logo.jpg"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
